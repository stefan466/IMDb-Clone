<template>

 <div>
    <b-pagination
      v-model="currentPage"
      :total-rows="genres.length"
      :per-page="perPage"
      aria-controls="image-table"
    ></b-pagination>

    <b-table
      id="image-table"
      hover
      fixed
      :items="genres"
      :fields="fields"
      small
      :per-page="perPage"
      :current-page="currentPage"
      @row-clicked="rowClicked"
    >
    </b-table>
    <b-pagination
      v-model="currentPage"
      :total-rows="genres.length"
      :per-page="perPage"
      aria-controls="image-table"
    ></b-pagination>
  </div>

</template>

<script>
  import { mapState } from 'vuex';
  export default {
    name: "GenreList",
    data() {
      return {
        fields: ['id', 'genre_title'],
        items: [],
        currentPage: 1,
        perPage: 10
      }
    },
    mounted() {
    },
    computed: {
      ...mapState([
        'genres'
      ])
    },
    methods: {
      rowClicked(record, index) {
        this.$router.push({ name: 'SingleGenre', params: { id: record.id} });
      }
    }
  }
</script>

<style scoped>
  .pagination {
    justify-content: center;
  }
  
</style>